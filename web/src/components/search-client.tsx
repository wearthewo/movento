"use client";import {useMemo,useState} from "react";import {Search} from "lucide-react";import {demoTitles} from "@/lib/demo-data";import {TitleCard} from "./title-card";
export function SearchClient(){const [query,setQuery]=useState("");const results=useMemo(()=>demoTitles.filter(x=>(x.title+" "+x.genres.join(" ")).toLowerCase().includes(query.toLowerCase())),[query]);return <><label className="relative block"><Search className="absolute left-4 top-1/2 size-5 -translate-y-1/2 text-muted-foreground"/><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Titles, people and genres" className="w-full rounded-lg border bg-card py-4 pl-12 pr-4 text-lg outline-none focus:ring-2 focus:ring-primary"/></label><p className="my-6 text-sm text-muted-foreground">{query?`${results.length} result${results.length===1?"":"s"}`:"Popular searches"}</p><div className="grid grid-cols-2 gap-5 sm:grid-cols-3 md:grid-cols-5 lg:grid-cols-6">{results.map(x=><TitleCard key={x.id} item={x}/>)}</div></>}
