import type {MetadataRoute} from "next";import {demoTitles} from "@/lib/demo-data";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_APP_URL??"http://localhost:3000";return [{url:base,lastModified:new Date(),priority:1},{url:`${base}/browse`,lastModified:new Date(),priority:.9},...demoTitles.map(x=>({url:`${base}/title/${x.slug}`,lastModified:new Date(),priority:.7}))]}
