import {TitleCard} from "@/components/title-card";import {getTitles} from "@/lib/api";export const metadata={title:"Movies"};export default async function Movies(){const items=await getTitles("MOVIE");return <main className="px-5 pb-20 pt-28 md:px-12"><h1 className="mb-8 text-4xl font-black">Movies</h1><div className="grid grid-cols-2 gap-5 sm:grid-cols-3 md:grid-cols-5 lg:grid-cols-6">{items.map(x=><TitleCard key={x.id} item={x}/>)}</div></main>}
